org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.suppressUnsupportedCompileSdk=35

# Pinned to a JDK 17 install rather than Android Studio's bundled JBR: that
# JBR auto-updates itself and jumped to JDK 25, which this project's Kotlin
# 2.0.0 / KSP toolchain can't parse (fails with "IllegalArgumentException:
# 25.0.2" during kspDebugKotlin). Update this path if this JDK is ever moved.
org.gradle.java.home=C:/Users/User/.jdks/ms-17.0.20
